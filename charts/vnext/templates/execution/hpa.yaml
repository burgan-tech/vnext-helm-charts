{{- if .Values.execution.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "vnext.fullname" . }}-execution
  labels:
    {{- include "vnext.labels" . | nindent 4 }}
    app.kubernetes.io/component: execution
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "vnext.fullname" . }}-execution
  minReplicas: {{ .Values.execution.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.execution.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.execution.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.execution.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.execution.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.execution.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
